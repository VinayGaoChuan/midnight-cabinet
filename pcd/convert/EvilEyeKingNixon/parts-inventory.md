# 邪眼王尼克松 EvilEyeKingNixon · 可复用画法清单

模块：`pcd/chars/EvilEyeKingNixon.js`（行号以该文件为准）。坐标以脚底为原点、面朝右；眼球中心在 (0, −25)，悬浮时整体下移 `P.by`（`begin` 的 y 偏移），地面在本地 `gy = −P.by`。

## 材质（原版下标 → 颜色）

| 材质 | 色阶 [勾线, 暗, 基, 亮] | band / flat | 用在 |
|---|---|---|---|
| `M_SCL` / `M_SMALL` | 27 #3a2e10 · 28 #7a6424 · 29 #c2a848 · 30 #eee08c（病黄眼白） | band 2 / 1 | 大眼白 / 小眼 |
| `M_FLESH` / `M_LID` / `M_TENT` | 31 #1a1020 · 32 #33243d · 33 #4f3a5c · 34 #6e5580（紫灰腐肉） | band 2 / 1 / 1 | 肉球 / 眼睑 / 触须（触须不吃轮廓光） |
| `M_ROBE` | 35 #0c1410 · 36 #1c2a22 · 37 #2e4236 · 37 | band 2 | 王袍（亮面沿用基色，只有基 + 暗两色） |
| `M_CROWN` | 0 · 20 · 38 #8a5a1e · 14 | band 1 | 王冠、袍边金线 |
| `M_VEIN` | 12 · 12 · 13 · 39 #d8281a | flat | 血丝（tone 3 = 暗红，tone 4 = 狂怒红）、冠上红宝石 |
| `M_SPEC` | 5 | flat | 眼白湿高光、冠尖闪 |
| `M_IRIS` / `M_ACID` | 43 #24300a · 42 #6a8a14 · 41 #c8e030 · 40 #f4ffb0 | flat | 虹膜（发光体）/ 酸液水洼（同色、分开材质） |
| `M_GLOW` | 40 · 40 · 21 · 21 | flat | 施放白芯、冠齿全白 |
| `M_INK` | 0 | flat | 瞳孔 |

特效色阶：`bile` 21 → 40 → 41 → 42 → 43（元素）· `wardGold` 21 → 5 → 14 → 38 → 20（护壁碎片）。护壁点直接用 38（暗金）/ 14 / 20 / 21。

## 身体部件（悬浮巨眼）

| 名字 | 画的是什么 | 参数 | 材质 | 画法要点 | 函数 / 行号 |
|---|---|---|---|---|---|
| 破王袍披风 | 从冠两端垂下、绕到眼球后面的斗篷，下摆两角最低 | 行 `yT −34 … −8`；`P.sway` 下摆摆动（按 t² 加权）；地面 `gy` 截断 | `M_ROBE`、`M_CROWN` | 左右边 = −9 − 10√t / 7 + 8√t；下摆弧 `robeHem`（中间收在眼球后、两角低 6 格）+ 锯齿 `HEM_JAG`；下摆上一行是金边，每 7 格缺一格；三道褶 tone 2；3 个破洞（跟着 sway） | `drawStanding` 180–191、`robeHem` 131 |
| 触须 | 从根部逐格往下长的触须，尖端卷曲、碰地横铺 | 根 `x0, y0`，长 `L`，相位 `ph`，卷向 `cd`，地面 `gy`，第几条 `fwd`；`P.tmode` 形态、`P.tph` 相位 | `M_TENT` | 每格按角度 `th` 走一步（th = 与竖直向下的夹角），th 每步加 `bend + amp·sin(相位 − k/2)`，最后 4 格加卷曲；前 45% 两格粗、每 3 格一个吸盘（tone 4）；形态表：0 垂 · 1 半卷 · 2 全卷 · 3 后拖 · 4 抽搐 · 5 瘫地 · 6 伸出去卷 · 7 张开 · 8 前甩 | `tent` 157；排布表 `TENTS` 125（前两条在后，各自一个 `part()`，192） |
| 眼球肉球 | 包住眼白的厚眼睑肉球 | 半径 11.5 + `P.swell`；触地压扁 `P.sq`（横 +sq、竖 −sq） | `M_FLESH` band 2 | 椭圆 `u² + v² ≤ 1.02`；褶痕 `FOLDS`（tone 2 一行 + 上方 tone 4 一格）；轮廓两个缺口；闭眼（lid ≥ 3）时小眼画成 tone 1 缝 | `drawStanding` 195–199 |
| 眼白 | 偏右的椭圆眼白 + 暗面抖动 + 血丝 + 湿高光 | 中心偏右 1 格、半径 8.5 × 8；`P.vein` 0–2 | `M_SCL`、`M_VEIN`、`M_SPEC` | 右下暗面（u·0.6 + v·0.8 > 0.62）棋盘格 tone 2；血丝 `VEINS0` 19 格（vein 1 时隔一个变红，2 全红），`VEINS1` 再长 16 格；左上 3 格湿高光 | `inScl` 147、`drawStanding` 200–207 |
| 虹膜 + 竖缝瞳孔 | 三圈虹膜、4 档瞳孔、1 格高光（发光体） | 偏移 `P.ix, P.iy`；档位 `P.gem` 0–4（`IRIS_LV`）；瞳孔 `P.pw` 0–3（`PUPIL` 细缝 / 竖缝 / 放大 / 圆） | `M_IRIS`、`M_GLOW`、`M_INK` | 半径 4.4 / 3.3 / 2 分三圈，每档给出三圈的材质 + tone；全部裁在眼白里；瞳孔 tone 1；左上高光（glint 时换 `M_GLOW`） | `drawStanding` 208–212、表 132–145 |
| 眼睑 | 上睑厚褶往下盖、下睑 2 行、闭眼时一道睫线 | `P.lid` 0–4 | `M_LID` | 在眼球椭圆（半径 9.5 × 9）里、肉球轮廓内，画掉 `top` 以上和 `bot` 以下；`top = cy − 7 + 0.9·cur + lid/4·16`（cur 让中间更低）；lid 4 整片盖住 + tone 1 睫线 | `drawStanding` 213–221 |
| 小眼 ×3 | 眼睑褶里的小眼，跟着大眼转 | `SMALL_EYES [x, y, 宽]`；瞳孔随 `P.ix / 3` 移动，`P.iy` ≥ 2 往下 | `M_SMALL`、`M_INK` | 两行（上行左端 tone 4、下行右端 tone 2），各自一个 `part()`；lid ≥ 3 不画（改由肉球里的 tone 1 缝表示） | `drawStanding` 222–226 |
| 尖齿王冠 | 3 行冠圈 + 5 个高矮相间的尖齿 + 两颗红宝石 + 中间酸绿石 | 冠圈左下角 `X, Y`，整 90° 旋转 `rot`，歪斜 `tilt`（每列往上 `u × tilt`），地面 `gy` 截断；`P.cg` 0 常亮 / 1 中齿闪 / 2 全齿白 | `M_CROWN`、`M_VEIN`、`M_IRIS`、`M_GLOW`、`M_SPEC` | `CROWN_TEETH [u, 高]`，每齿 2 格宽、尖 1 格；所有像素经 `put2` 做歪斜 + 旋转，方便掉落翻滚；站姿往后歪戴 0.18，受击 `P.cj` 跳起 1 格 | `crownAt` 149 |
| 站姿骨架（悬浮） | 部件顺序 | — | — | 王袍 → 5 条触须（后两条先）→ 肉球 → 眼白 → 虹膜瞳孔 → 眼睑 → 3 只小眼 → 王冠 | `drawStanding` 178 |
| 破裂皮囊（最终姿） | 撕开的破口、耷拉的眼白碎片、熄灭的扁虹膜、瘫地触须、塌下的王袍、酸液水洼；王冠滚到一边 | `P.crX / crY / crR`（王冠滚动、4 = 斜靠）；`P.gem` 虹膜档 | 全部 | 水洼 → 王袍堆（半椭圆 + 锯齿 + 金边）→ 5 条瘫开的触须 `SPLAY`（尖端上翘）→ 瘪肉球（顶上按 `TEAR` 撕开，露出 tone 1 内壁）→ 眼白碎片 + 暗红血丝 → 扁虹膜 + 瞳孔缝 → 王冠 | `drawLying` 233 |

## 姿势 / 时间表（可复用的节奏）

| 名字 | 内容 | 行号 |
|---|---|---|
| 飘浮步态 | `FLOAT_BY [0, −1, −2, −1]`，6 fps，不落地；触须 `tmode 3` 后拖 | 43、`poseAt` 移动段 |
| 待机乱瞟 + 慢眨 | `LOOK [起点秒, ix, iy]` 5 段、`BLINK [起点秒, lid]` 9 段（约 0.7 s 眨一次） | 44–45 |
| 坠地回弹 | `DEATH_BOUNCE [起点, by, 压扁]`：触地压扁 1 → 回弹 4 格 → 再落地压扁 | 49 |
| 连射节奏 | `B_T [0.1, 0.26, 0.36]`（间隔 0.16 → 0.10，越打越快），每发后坐 1 格 | 48 |

## 特效图案

| 名字 | 画的是什么 | 参数 | 色阶 | 画法要点 | 函数 / 行号 |
|---|---|---|---|---|---|
| 凝视光束 | 瞳孔到目标的直线光束，3 种宽度 | 起止点、宽度档 `w`（0 = 1 格 · 1 = 3 格 · 2 = 4 格 + 外沿）；寿命 2 帧 | 元素 | Bresenham；第 1 帧白芯 + 第 2 级两侧（w 0 每 4 格一个上下错开的闪点），第 2 帧降一级、每 3 格断一格；第 1 帧末端加小十字；最多 3 道 | `beam` 290、`drawBeam` 368 |
| 瞳孔口光 | 出手时瞳孔上的小十字 | 位置；2 帧 | 元素 1–3 级 | 右 / 上 / 下各 2 格，左边 2 格降一级 | `fxFront` 380 |
| 施放竖直眼光 | 瞳孔上下拉出的竖光 + 左右短芒 | 施放 1/12–0.25 s | 元素 | 上下各 r = 5–9（5–6 第 1 级、7–8 第 2 级、9 第 3 级），左右 5–6 | `fxFront` 380 |
| 虹膜外沿四角闪点 | 蓄力 2 档时虹膜外 4 个角的闪点 | `P.gem === 2` | 元素 2 / 3 级逐帧交替 | ±5, ±4 | `fxFront` 380 |
| 首领护壁 | 身周椭圆点阵护壁 | 半径 19 × 22、64 个点位；模式：平时 / 施放全亮 / 受击右段白 1 帧 / 死亡整圈白 1 帧 | 38 暗金、14、20、21 | 平时：每 4 点一个 38，其余 20，一颗 14 亮点每 2 帧沿圈走一格；施放：38 / 14 交替；受击 `cos > 0.55` 的一段变 21；不画进地面；死亡时碎成 32 颗 `wardGold` 碎片（`onTime` 319） | `drawWard` 343 |
| 悬浮投影 | 身下地面线上的墨色影子 | 半宽 8 + by/4（倒地 14） | 0 | 外沿 2 格隔点，第二行更窄隔点 | `fxBack` 360 |
| 地面映光（自定义宽度） | 瞳孔下方地面映光 | rim 2：半宽 6 隔点；rim 3：半宽 12 实线，中间 5 格第 2 级 | 元素 | 比引擎 `floorGlow`（7 / 14、中间 6 格）窄一点，保留原版 | `fxBack` 360 |
| 魂丝（定点螺旋吸入） | 4 处源头连续发射、两两反向旋的螺旋粒子，连成 4 条弯曲魂丝 | 源头 `THR`；每处每秒 40 颗；速度 r / (0.5 + j·0.03)，角速度 ±1.3 | 元素 | `K_SPIRAL_PT` 定点吸向发射时的瞳孔；每 3 颗有 1 颗到达后转成绕瞳孔半径 6–7 的环绕（引擎不支持，模块按到达时刻补 `K_ORBIT`，见 engineRequests） | `stepFX` 329、`queueOrbit` 281、`stepOrbitQueue` 287 |
| + 字回血粒子 | 5 颗一组同速上升的十字 | 6 组，速度 −18…−28，寿命 0.6–0.9 | 元素 | `K_TRAIL`、vx = 0（只有纵向阻力） | `onEnter` 296、`PLUS` 293 |
| 酸绿溅液 | 眼球破裂时往上喷、落地贴住的溅液 | 40 颗，扇形 −0.06π…−0.94π，速度 45–140，重力 170 | 元素 | 引擎粒子池没有这种粒子，模块里单独一个小池（见 engineRequests） | `splat` 265、`stepSplats` 270、`drawSplats` 277 |
